 # JavaScript Main Concepts.  

      1. Instance Objects
      2. Functions
      3. DOM Manipulation
      4. Local Storage 
      5. Async Javascript 


## 1. Instance Objects  

In Javscript there are some confusions regarding objects, because there is object data type and instance objects. 

### Object Data types
So object data types are data types that store data as a key-value pairs. Where keys are uniqe and made up of strings and symbols, values can be of any data type. 
This data type can be creted using either the object literal or new keyword.  

 
 
